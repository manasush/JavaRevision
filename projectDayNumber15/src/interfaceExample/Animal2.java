package interfaceExample;

public interface Animal2 extends Animal {
	void ear(); // By default, all methods in an interface are public and abstract

	void ear2(); // By default, all methods in an interface are public and abstract
}